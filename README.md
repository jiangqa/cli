cli 工具 对内
```
   npm install -g xf-vue-cli //安装
```

```
  xf-vue-cli project //拉取项目，维护常用项目，省去查找项目地址的烦恼
  xf-vue-cli init <project-name> //创建新项目，admin,h5 2套模版
  xf-vue-cli page <name> <path> //新建page vue页面并添加到路由
  xf-vue-cli component <name> <path> //新建component组件
```

